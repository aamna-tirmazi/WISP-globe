#### WISP-globe
This repository contains Python code that generates an interactive Plotly globe visualizing the geographic distribution of Wien International Scholars by country. The visualization is populated using a CSV dataset. When executed, the script exports the interactive globe as an HTML file, which can be embedded into the Brandeis ISSO website.
